import unittest
import cached_chart
import cached_music
from FinalProj import *

class TestBillboard(unittest.TestCase):

    def setup(self):
        self.test_chart = get_billboard()
        self.output_chart = print(str(counter) + '. ' + ''.join(billboard_dict[counter]))

    def 
    def billboardSize(self):
        self.assertEqual(len(test_chart.keys), 100)
    def
    def caching_results(self):
    def billboard_ordered(self):
